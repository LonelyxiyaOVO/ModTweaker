# ModTweaker CRT Documentation

The API reference is available in [CRT_API.md](CRT_API.md).

Pages use the same basic layout as the BlameJared 1.12 documentation:

1. Package
2. Addition
3. Removal
4. CRT script example

Use `<ore:ingotIron>` for ore-dictionary entries and `<liquid:water>` for
fluids in CRT scripts.
